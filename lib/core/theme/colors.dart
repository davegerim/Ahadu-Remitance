import 'package:flutter/material.dart';

class AppPalette {
  // Ultra-clean backgrounds
  static const Color background = Color(0xFFF7F7F9);
  static const Color surface = Color(0xFFFFFFFF);
  
  // High-contrast dark elements (for premium fintech look)
  static const Color surfaceDark = Color(0xFF121212);
  static const Color surfaceDarker = Color(0xFF0A0A0A);

  // Ahadu Brand Colors
  static const Color primary = Color(0xFF8A1538);
  static const Color primaryLight = Color(0xFFFDEEF1);
  static const Color primaryDark = Color(0xFF6B0F2A);

  // Typography
  static const Color textPrimary = Color(0xFF121212);
  static const Color textSecondary = Color(0xFF71717A);
  static const Color textHint = Color(0xFFA1A1AA);

  // States & Accents
  static const Color success = Color(0xFF10B981);
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);

  // Borders
  static const Color border = Color(0xFFE4E4E7);
  static const Color borderLight = Color(0xFFF4F4F5);
}
